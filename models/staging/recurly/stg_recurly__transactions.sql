SELECT ACCOUNT:code::STRING     AS ACCOUNT_CODE,
       ACCOUNT:id::STRING       AS ACCOUNT_ID,
       INVOICE:id::STRING       AS INVOICE_ID,
       INVOICE: NUMBER::INTEGER AS INVOICE_NUMBER,
       SUBSCRIPTION_IDS,
       ID                       AS TRANSACTION_ID,
       NVL(AMOUNT, AMOUNT__DE)  AS AMOUNT,
       AVS_CHECK,
       BILLING_ADDRESS,
       COLLECTED_AT,
       COLLECTION_METHOD,
       CREATED_AT,
       CURRENCY,
       CUSTOMER_MESSAGE,
       CUSTOMER_MESSAGE_LOCALE,
       CVV_CHECK,
       GATEWAY_MESSAGE,
       GATEWAY_REFERENCE,
       GATEWAY_RESPONSE_CODE,
       GATEWAY_RESPONSE_TIME,
       GATEWAY_RESPONSE_VALUES,
       IP_ADDRESS_COUNTRY,
       IP_ADDRESS_V4,
       OBJECT,
       ORIGIN,
       ORIGINAL_TRANSACTION_ID,
       PAYMENT_GATEWAY,
       PAYMENT_METHOD,
       REFUNDED,
       STATUS,
       STATUS_CODE,
       STATUS_MESSAGE,
       SUCCESS,
       TYPE,
       UUID,
       VOIDED_AT
FROM {{ source('RECURLY', 'TRANSACTIONS') }} T